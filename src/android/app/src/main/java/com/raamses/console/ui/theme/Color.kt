package com.raamses.console.ui.theme

import androidx.compose.ui.graphics.Color

// ── Status Colors ──
val StatusActive = Color(0xFF00E676)        // bright green
val StatusQuiet = Color(0xFFFFCA28)         // amber
val StatusIdle = Color(0xFF78909C)          // blue-grey
val StatusStale = Color(0xFFFF7043)         // deep orange
val StatusBlocked = Color(0xFFFF1744)       // red
val StatusUnverified = Color(0xFFAA00FF)    // purple

// ── Severity Colors ──
val SeverityCritical = Color(0xFFFF1744)
val SeverityWarning = Color(0xFFFF9100)
val SeverityInfo = Color(0xFF448AFF)

// ── OLED-Optimized Dark Theme ──
val Background = Color(0xFF0A0A0A)          // near-true black
val Surface = Color(0xFF141414)
val SurfaceVariant = Color(0xFF1E1E1E)
val CardBackground = Color(0xFF1A1A1A)
val Border = Color(0xFF2A2A2A)

// ── Text ──
val TextPrimary = Color(0xFFE8E8E8)
val TextSecondary = Color(0xFF9E9E9E)
val TextMuted = Color(0xFF616161)

// ── Accent ──
val AccentBlue = Color(0xFF448AFF)
val AccentGreen = Color(0xFF00E676)
val AccentOrange = Color(0xFFFF9100)
val AccentRed = Color(0xFFFF1744)

// ── Command Input ──
val CommandBarBackground = Color(0xFF1A1A1A)
val CommandCursor = Color(0xFF00E676)
val SlashHighlight = Color(0xFF448AFF)
